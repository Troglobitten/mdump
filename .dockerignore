# Dependencies
node_modules/
.pnpm-store/

# Git
.git/
.gitignore

# IDE
.idea/
.vscode/
*.swp
*.swo

# Documentation
*.md
!README.md

# Local environment
.env.local
.env.*.local

# Build artifacts (we build inside container)
dist/
build/

# Development files
.coverage/
coverage/
*.log
*.local

# Docker files (not needed inside container)
docker/
Dockerfile*
docker-compose*
# ...except the entrypoint script, which the image needs
!docker/entrypoint.sh

# Data (mounted as volume) — match at any depth (e.g. server/data), not just root
**/data/
**/.search-index.json
**/.image-cache/

# OS files
.DS_Store
Thumbs.db
